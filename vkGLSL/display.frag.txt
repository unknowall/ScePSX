#version 450

// 输入纹理坐标，从顶点着色器传递过来
layout(location = 0) in vec2 TexCoord;

// 输出颜色
layout(location = 0) out vec4 FragColor;

// 均匀变量
layout(binding = 0) uniform sampler2D tex; // 纹理采样器

void main()
{
    // 采样纹理并输出颜色
    FragColor = texture(tex, TexCoord);
}